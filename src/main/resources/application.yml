# DB setting
spring:
  datasource:
    url: jdbc:mysql://localhost:3306/culendar?useUnicode=true&characterEncoding=UTF-8&characterSetResult=UTF-8&useSSL=true
    username: root
    password: ncc-1701
    devtools:
      # Auto Restart
      restart:
        enabled: true
        additional-exclude: static/**,public/**
#  thymeleaf:
#    cache: false

  jpa:
    database: mysql
    hibernate:
      ddl-auto: update
#    show-sql: true
    properties:
      hibernate:
        format_sql: true
        highlight_sql: true

#logging:
#  level:
#    org.hibernate.sql: debug
#    org.hibernate.orm.jdbc.bind: trace

server:
  servlet:
    session:
      tracking-modes: cookie